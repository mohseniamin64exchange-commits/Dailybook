# وضعیت فعلی پروژه

آخرین بروزرسانی: ۱۴۰۵/۰۶/۲۸
نسخه: 1.0.0-rc5 (Design System نهایی Dark/Light اعمال شد؛ آماده تست بصری مرورگر)

## وضعیت پروژه
- Design System تأییدشده کاربر روی رابط واقعی اعمال شده است
- Admin: سایدبار ثابت سمت راست؛ User: بدون سایدبار و فقط ۱۰ ثبت اخیر خودش
- توقف پس از تحویل rc4 برای انتقال به اکانت دارای دسترسی GitHub
- Build آنلاین GitHub و تست Windows به اکانت/عامل بعدی سپرده می‌شود

## آخرین Task تأییدشده
- [x] اعمال طراحی نهایی Dark/Light، سایدبار ثابت Admin و داشبورد بدون سایدبار User

## Task فعلی
- [ ] 17.7 تأیید بصری تغییرات rc5 توسط کاربر در مرورگر واقعی

## سه Task بعدی
- [ ] انتقال نسخه اصلاح‌شده به اکانت دارای دسترسی GitHub
- [ ] 16.7 ساخت فایل نهایی `DailyBook-Setup-x64.exe` روی Windows x64
- [ ] 16.8 تست Clean Install، Upgrade و Uninstall روی Windows 10/11

## فایل‌های تغییرکرده در آخرین مرحله
- `app/templates/entries/dashboard.html` برای چیدمان فرم و عنوان ثبت‌های اخیر
- `app/static/css/app.css` و قالب ورود برای رنگ و کادرهای سراسری
- `app/templates/base.html` و قالب‌های Admin برای هدر و منوی همبرگری
- `app/templates/admin/report.html` برای ساعت، نام کامل کاربر و جدول‌بندی کامل
- `tests/test_admin.py`، Changelog و نسخه Installer

## تست‌های انجام‌شده
- ۳۱ تست نسخه rc4 قبلاً موفق بود. تست rc5 در محیط فعلی اجرا نشد چون Flask در محیط نصب نیست و دسترسی شبکه برای نصب dependency وجود ندارد.
- تست جدید اضافه شد که نبودن سایدبار Admin برای User عادی را کنترل می‌کند.
- compile کامل Python و بررسی whitespace تغییرات بدون خطا است.
- ترتیب هفت گزینه منوی مدیر و ستون‌های جدید گزارش تست خودکار دارند.

## خطاها / Blockerها
- محیط توسعه فعلی Linux است و کامپایل واقعی Inno Setup/PyInstaller Windows در آن ممکن نیست.
- به درخواست کاربر از GitHub استفاده نمی‌شود؛ بنابراین Build نهایی باید یک‌بار با `BUILD_WINDOWS_SETUP.bat` روی Windows x64 انجام شود.
- تست نصب/Upgrade/Uninstall و LAN فقط روی سیستم مقصد قابل انجام است.
- Build نهایی تا تأیید بصری rc5 متوقف است.

## تصمیم جدید
- کاربر نهایی فقط یک فایل Setup دریافت می‌کند و هیچ پیش‌نیاز یا فرمان دستی اجرا نمی‌کند.
- برنامه در `C:\Program Files\DailyBook` نصب و داده‌ها در `C:\ProgramData\DailyBook` نگهداری می‌شوند.
- سرویس DailyBook با Windows خودکار اجرا می‌شود؛ پورت پیش‌فرض ۴۰۰۰ و از پنل مدیر قابل تغییر است.

## شمارش پیشرفت
- انجام‌شده: ۱۷۳ Micro Task
- باقی‌مانده واقعی: ۲۲ Micro Task میدانی/تحویلی

## اصلاح نهایی رابط rc5 — ۱۴۰۵/۰۶/۲۹
- سایدبار مدیریتی فقط برای حساب Admin روی خود کامپیوتر سرور/localhost نمایش داده می‌شود.
- Admin در اتصال LAN و همه Userهای عادی پوسته بدون سایدبار دریافت می‌کنند.
- User عادی فقط فرم ثبت و ۱۰ رکورد اخیر متعلق به خودش را می‌بیند.
- تست ساختاری برای مخفی بودن Sidebar در Remote Admin اضافه شد.
- `compileall` برای app و tests بدون خطا اجرا شد؛ اجرای pytest در محیط فعلی به‌دلیل نبود Flask dependency ممکن نشد.

- [x] نمایش عنوان رویدادهای Audit Log در رابط مدیر به فارسی انجام شد؛ مقدار استاندارد انگلیسی در دیتابیس بدون تغییر باقی می‌ماند.

- [x] ساده‌سازی ستون جزئیات Audit Log: نمایش JSON و کلیدهای فنی حذف و اطلاعات قبل/بعد با برچسب‌های فارسی و خوانا نمایش داده می‌شود.


## rc5.4 UI
تمام صفحات منوی مدیریت با بیس رنگ، کارت‌ها، هدر صفحه، فرم‌ها، جدول‌ها و اکشن‌های هماهنگ با داشبورد نهایی بازسازی شدند. منطق Backend و داده‌ها بدون تغییر باقی مانده است.


## Windows Installer readiness — rc5.4
- Build pipeline استاندارد Windows x64 آماده است.
- Inno Setup metadata با نسخه `1.0.0-rc5.4` همگام شد.
- خروجی هدف: `installer/output/DailyBook-Setup-x64.exe`.
- تولید واقعی EXE نیازمند اجرای build روی Windows x64 است و در محیط Linux فعلی قابل انجام نیست.


## rc5.5 — Windows build compatibility
- Build pipeline دیگر به `py -3.11` وابسته نیست و Python نصب‌شده را خودکار انتخاب می‌کند.
- سیستم دارای Python 3.14.6 می‌تواند بدون نصب جداگانه Python 3.11 وارد مرحله ساخت شود.
- در نبود Python، bootstrap تلاش می‌کند Python 3.14 x64 را با winget نصب کند.


## rc5.6
- گزینه اجرای خودکار سرویس DailyBook پس از روشن/Restart ویندوز به تنظیمات Admin اضافه شد.
- حالت فعال، Startup Type سرویس را Automatic و حالت غیرفعال آن را Manual می‌کند.
- مقدار تنظیم در دیتابیس ذخیره می‌شود و تست‌های پذیرش برای UI و ذخیره مقدار اضافه شدند.
- تست گزارش با UI جدید هماهنگ شد؛ Assertion قدیمی «نام و نام خانوادگی کاربر» به عنوان فعلی «کاربر» اصلاح شد.


## وضعیت واقعی Build — 2026-09-20
- GitHub Actions Run #35495484693 با نتیجه سبز تأیید شد.
- تست‌ها و تمام مراحل Build موفق بودند.
- فایل نهایی `DailyBook-Setup-x64.exe` به‌عنوان Artifact با نام `DailyBook-Setup-x64` موجود است.
- SHA-256 گزارش‌شده توسط GitHub برای Artifact: `bd17f67b4fdbb7525ecee92a85ae549d7f8c8ceff73077b0eed595e92aaa531b`.
- اصلاح ضروری Build: رفع resolve شدن مسیر `ISCC.exe` در PowerShell؛ منطق و رابط کاربری برنامه تغییر نکرده است.
