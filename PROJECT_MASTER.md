# دفتر روزانه تحت شبکه — سند مادر پروژه و رودمپ

> **این فایل منبع اصلی حقیقت پروژه (Single Source of Truth) است.**
> هر حساب ChatGPT یا توسعه‌دهنده‌ای که پروژه را ادامه می‌دهد باید قبل از هر تغییری این فایل را کامل بخواند.
> در هر جلسه فقط روی «مرحله بعدیِ تیک‌نخورده» کار شود و پس از تأیید کاربر، همان مورد تیک بخورد.

---

## 1) هدف پروژه

ساخت یک «دفتر روزانه تحت شبکه داخلی» بسیار ساده، سریع و فارسی برای 3 تا 5 کاربر.

این برنامه **نرم‌افزار حسابداری کامل نیست**. هدف فقط ثبت سریع درآمد/هزینه و تولید دفتر روزانه کلاسیک با ستون‌های بدهکار و بستانکار است.

### اصل طراحی
- کمترین تعداد فیلد
- کمترین کلیک
- سریع با ماوس و کیبورد
- فقط شبکه داخلی (LAN)
- بدون Cloudflare و بدون دسترسی اینترنتی
- رابط فارسی، راست‌به‌چپ، اعداد فارسی، تاریخ شمسی

---

## 2) تصمیم‌های قطعی پروژه

### معماری
- برنامه به‌صورت Web App داخل شبکه داخلی اجرا می‌شود.
- یک کامپیوتر نقش سرور را دارد.
- کاربران با مرورگر کامپیوتر یا موبایل داخل شبکه وارد می‌شوند.
- دیتابیس: **SQLite**
- فایل SQLite فقط توسط برنامه سرور استفاده می‌شود؛ کاربران نباید فایل دیتابیس را مستقیماً از Share شبکه باز کنند.

### کاربر اولیه
- در اولین اجرای برنامه، اگر هیچ کاربری وجود نداشت، صفحه ساخت «ادمین اصلی» نمایش داده شود.
- ادمین اصلی با این موارد ساخته می‌شود:
  - نام
  - نام خانوادگی
  - Username
  - Password
- پس از ایجاد ادمین اصلی، این صفحه دیگر نمایش داده نشود.

### نقش‌ها
#### Admin اصلی
- دسترسی به همه قسمت‌ها
- ساخت کاربر
- ویرایش مشخصات کاربر
- فعال/غیرفعال کردن کاربر
- حذف کاربر
- تعیین/ریست رمز کاربران
- باز کردن حساب قفل‌شده
- دسترسی به همه ثبت‌ها
- دسترسی به گزارش‌ها
- دسترسی به چاپ
- دسترسی کامل به لاگ
- امکان حذف/پاک‌سازی لاگ
- تنظیمات بکاپ

#### User
- ورود به سیستم در صورت فعال و قفل‌نبودن
- ثبت درآمد/هزینه
- مشاهده ثبت‌های اخیر خودش
- ویرایش ثبت‌های خودش
- حذف ثبت‌های خودش
- عدم دسترسی به گزارش‌های کلی
- عدم دسترسی به لاگ
- عدم دسترسی به مدیریت کاربران
- عدم امکان تغییر رمز شخصی

---

## 3) مدیریت کاربران

هر کاربر شامل موارد زیر است:
- نام
- نام خانوادگی
- Username
- Password
- Role
- Status

### قوانین
- Username باید **Unique** باشد.
- Password فقط توسط Admin تعیین یا ریست می‌شود.
- User نمی‌تواند رمز خودش را عوض کند.
- پس از **5 بار رمز اشتباه**، اکانت Lock شود.
- فقط Admin می‌تواند Lock را Reset کند.

### وضعیت کاربر
- **Active:** امکان ورود و ثبت دارد.
- **Inactive:** اکانت باقی است ولی امکان ورود ندارد؛ Admin می‌تواند دوباره فعال کند.
- **Deleted:** از فهرست کاربران حذف می‌شود.

### حذف کاربر
حذف اکانت نباید هیچ سابقه قبلی را پاک کند.
در ثبت‌ها و لاگ‌ها باید نام/نام خانوادگی/Username کاربر در زمان انجام عملیات باقی بماند، حتی اگر اکانت او بعداً حذف شود.

---

## 4) فرم اصلی ثبت

فرم باید بسیار ساده باشد و فقط این موارد را داشته باشد:

1. **تاریخ**
   - پیش‌فرض: تاریخ همان روز
   - هجری شمسی
   - قابل تغییر
   - کنار فیلد، آیکون تقویم شمسی
   - انتخاب تاریخ‌های دیگر از تقویم

2. **نوع**
   - فقط دو گزینه:
     - هزینه
     - درآمد

3. **شرح**
   - متن آزاد

4. **مبلغ**
   - فقط عدد
   - هنگام تایپ به‌صورت خودکار سه‌رقم سه‌رقم جدا شود
   - نمایش اعداد فارسی

5. **دکمه ثبت**

### رفتار ثبت
- هزینه → در خروجی دفتر در ستون «بدهکار»
- درآمد → در خروجی دفتر در ستون «بستانکار»
- ساعت واقعی ثبت به‌صورت خودکار ذخیره شود.
- تاریخ ثبت انتخابی کاربر و زمان واقعی ثبت دو داده جدا باشند.

### بعد از ثبت موفق
- Popup کوچک باز شود.
- ظاهر موفقیت سبز باشد.
- متن: «ثبت شد»
- دکمه: «تأیید»
- پس از تأیید Popup:
  - Popup بسته شود.
  - فوکوس مستقیم به فیلد **شرح** برگردد.
- لازم نیست آخرین ثبت حفظ یا تکرار شود.

---

## 5) کار با کیبورد

کل فرم باید بدون نیاز به ماوس قابل استفاده باشد.

- Tab بین فیلدها حرکت کند.
- ترتیب Tab منطقی باشد:
  تاریخ → نوع → شرح → مبلغ → ثبت
- Enter در جای مناسب ثبت را انجام دهد.
- بعد از ثبت و تأیید Popup، فوکوس روی شرح باشد.
- هیچ کنترل مهمی فقط با ماوس قابل استفاده نباشد.

---

## 6) ظاهر و زبان رابط

- زبان: فارسی
- Direction: RTL
- تاریخ: هجری شمسی
- تمام اعداد قابل مشاهده: فارسی
- همه فرم‌ها: وسط‌چین
- Labelها: وسط‌چین
- Headerها: وسط‌چین
- متن داخل Inputها: وسط‌چین
- متن دکمه‌ها: وسط‌چین
- جدول‌ها: متناسب با RTL و فارسی
- رابط باید ساده، تمیز و خلوت باشد.

---

## 7) ثبت‌های اخیر کاربر

پایین فرم اصلی، آخرین **5 یا 10 ثبت همان کاربر** نمایش داده شود.

برای هر رکورد:
- تاریخ
- نوع
- شرح
- مبلغ
- ساعت ثبت
- ویرایش
- حذف

### ویرایش
- User فقط ثبت خودش را ویرایش کند.
- هر ویرایش در Log ذخیره شود.
- مقدار قبلی و مقدار جدید ذخیره شوند.

### حذف
- User فقط ثبت خودش را حذف کند.
- قبل از حذف تأیید ساده نمایش داده شود.
- حذف در Log ثبت شود.
- جزئیات رکورد حذف‌شده در Log باقی بماند.

---

## 8) دفتر روزانه و گزارش

گزارش اصلی فقط برای Admin نمایش داده شود.

### شکل کلاسیک دفتر
ستون‌های اصلی:
- تاریخ
- شرح
- بدهکار
- بستانکار

قانون:
- Expense → Debit
- Income → Credit

### چاپ
- خروجی A4
- خروجی A5
- ظاهر چاپ می‌تواند با فرم ورود اطلاعات متفاوت باشد.
- چاپ باید شبیه دفتر روزانه کلاسیک و خوانا باشد.
- اعداد و تاریخ در چاپ فارسی/شمسی باشند.

---

## 9) Audit Log کامل

Log باید تا حد ممکن کامل باشد.

هر Event حداقل شامل:
- شناسه لاگ
- نوع رویداد
- User ID در صورت وجود
- Snapshot نام
- Snapshot نام خانوادگی
- Snapshot Username
- تاریخ و ساعت واقعی
- شناسه رکورد مرتبط در صورت وجود
- نتیجه عملیات (موفق/ناموفق)
- توضیح
- مقدار قبل (در تغییرات)
- مقدار بعد (در تغییرات)
- IP داخلی کلاینت در صورت دسترس
- خطا/پیام سیستم در صورت ناموفق بودن

### رویدادهایی که حتماً Log می‌شوند
- Login موفق
- Login ناموفق
- تعداد تلاش‌های ناموفق
- Lock شدن اکانت
- Unlock/Reset Lock توسط Admin
- Logout
- ایجاد User
- ویرایش User
- Active کردن User
- Inactive کردن User
- Delete کردن User
- تعیین/Reset Password توسط Admin
- ثبت درآمد/هزینه
- ویرایش ثبت
- حذف ثبت
- تغییر تاریخ
- تغییر نوع
- تغییر شرح
- تغییر مبلغ
- اجرای Backup دستی
- اجرای Backup خودکار
- موفق یا ناموفق بودن Backup
- تغییر مسیر Backup
- Fallback شدن Backup به مسیر پیش‌فرض
- تغییر تنظیمات برنامه
- پاک‌سازی/حذف Log توسط Admin
- خطاهای مهم برنامه

### دسترسی
- فقط Admin اصلی لاگ را می‌بیند.
- فقط Admin اصلی می‌تواند لاگ را حذف/پاک‌سازی کند.

---

## 10) Backup

### Backup دستی
- امکان گرفتن Backup دستی وجود داشته باشد.
- نتیجه موفق/ناموفق در Log ثبت شود.

### Backup خودکار
- برنامه Backup خودکار داشته باشد.
- مسیر پیش‌فرض: مسیر تعیین‌شده در نصب/داده برنامه.
- مسیر Backup خودکار قابل تغییر باشد.
- اگر مسیر سفارشی در دسترس نبود:
  1. عملیات Backup متوقف نشود.
  2. Backup در مسیر پیش‌فرض برنامه ذخیره شود.
  3. Event مربوطه در Log ذخیره شود.
  4. Admin از مشکل مسیر مطلع شود.

---

## 11) ساختار داده پیشنهادی

> نام دقیق فیلدها هنگام پیاده‌سازی نهایی می‌شود؛ منطق زیر قطعی است.

### Users
- id
- first_name
- last_name
- username (unique)
- password_hash
- role
- status
- failed_login_count
- is_locked
- created_at
- updated_at

### DailyEntries
- id
- entry_date
- entry_type (expense / income)
- description
- amount
- created_at
- updated_at
- created_by_user_id (در صورت موجود بودن)
- created_by_first_name_snapshot
- created_by_last_name_snapshot
- created_by_username_snapshot

### AuditLogs
- id
- event_type
- actor_user_id
- actor_name_snapshot
- actor_username_snapshot
- related_record_type
- related_record_id
- before_data
- after_data
- success
- message
- client_ip
- created_at

### Settings
- id / key
- backup_path
- auto_backup_enabled
- سایر تنظیمات محدود برنامه

---

# 12) رودمپ اجرایی — Micro Tasks

## فاز 0 — آماده‌سازی پروژه
- [x] 0.1 ساخت پوشه اصلی پروژه
- [x] 0.2 ساخت فایل README
- [x] 0.3 ذخیره همین PROJECT_MASTER.md داخل پروژه
- [x] 0.4 ساخت STATUS.md
- [x] 0.5 ساخت CHANGELOG.md
- [x] 0.6 راه‌اندازی Git محلی (`git init`)
- [x] 0.7 اولین Commit با عنوان `project bootstrap`
- [x] 0.8 انتخاب نهایی Backend/Framework
- [x] 0.9 مشخص کردن Port شبکه داخلی
- [x] 0.10 اجرای صفحه تست روی localhost
- [ ] 0.11 تست باز شدن صفحه از یک دستگاه دیگر داخل LAN

**Done when:** یک صفحه ساده از سرور روی حداقل یک دستگاه دیگر شبکه باز شود.

---

## فاز 1 — دیتابیس SQLite
- [x] 1.1 ساخت فایل SQLite
- [x] 1.2 ساخت جدول Users
- [x] 1.3 Unique کردن Username
- [x] 1.4 ساخت جدول DailyEntries
- [x] 1.5 ساخت جدول AuditLogs
- [x] 1.6 ساخت جدول Settings
- [x] 1.7 تست Insert/Read ساده
- [x] 1.8 Commit فاز دیتابیس

**Done when:** جداول بدون خطا ساخته شوند و یک داده تست ثبت/خوانده شود.

---

## فاز 2 — اولین Admin
- [x] 2.1 تشخیص خالی بودن جدول Users
- [x] 2.2 نمایش Setup Admin فقط در اولین اجرا
- [x] 2.3 فیلد نام
- [x] 2.4 فیلد نام خانوادگی
- [x] 2.5 فیلد Username
- [x] 2.6 فیلد Password
- [x] 2.7 Hash کردن Password
- [x] 2.8 ساخت Admin
- [x] 2.9 جلوگیری از نمایش مجدد Setup
- [x] 2.10 ثبت ایجاد Admin در Log
- [x] 2.11 Commit

**Done when:** اولین Admin ساخته شود و Setup دیگر باز نشود.

---

## فاز 3 — Login و Session
- [x] 3.1 ساخت صفحه Login
- [x] 3.2 ورود با Username/Password
- [x] 3.3 رد کردن Username اشتباه
- [x] 3.4 رد کردن Password اشتباه
- [x] 3.5 ثبت Login موفق در Log
- [x] 3.6 ثبت Login ناموفق در Log
- [x] 3.7 شمارش Failed Login
- [x] 3.8 Lock بعد از 5 تلاش اشتباه
- [x] 3.9 جلوگیری از Login کاربر Inactive
- [x] 3.10 جلوگیری از Login کاربر Locked
- [x] 3.11 ساخت Session
- [x] 3.12 Logout
- [x] 3.13 Log کردن Logout
- [x] 3.14 Commit

**Done when:** ورود، خروج، 5 خطای ورود و Lock کاملاً تست شوند.

---

## فاز 4 — مدیریت کاربران توسط Admin
- [x] 4.1 صفحه Users فقط برای Admin
- [x] 4.2 افزودن User
- [x] 4.3 کنترل Username تکراری
- [x] 4.4 ویرایش نام/نام خانوادگی
- [x] 4.5 Active کردن
- [x] 4.6 Inactive کردن
- [x] 4.7 Delete کردن
- [x] 4.8 حفظ Snapshot کاربر در سوابق قبلی
- [x] 4.9 Reset Password
- [x] 4.10 Unlock Account
- [x] 4.11 Log کردن تک‌تک عملیات
- [x] 4.12 Commit

**Done when:** Admin بتواند چرخه کامل User را مدیریت کند بدون حذف سابقه‌های قبلی.

---

## فاز 5 — فرم ثبت روزانه
- [x] 5.1 ساخت Layout RTL
- [x] 5.2 وسط‌چین کردن فرم
- [x] 5.3 تاریخ شمسی امروز به‌صورت پیش‌فرض
- [x] 5.4 آیکون تقویم
- [x] 5.5 انتخاب تاریخ شمسی دیگر
- [x] 5.6 Dropdown هزینه/درآمد
- [x] 5.7 فیلد شرح
- [x] 5.8 فیلد مبلغ
- [x] 5.9 سه‌رقمی شدن مبلغ هنگام تایپ
- [x] 5.10 نمایش اعداد فارسی
- [x] 5.11 دکمه ثبت
- [x] 5.12 ذخیره ساعت واقعی
- [x] 5.13 ذخیره Snapshot کاربر
- [x] 5.14 ثبت Event در Log
- [x] 5.15 Commit

**Done when:** یک User بتواند یک هزینه و یک درآمد ثبت کند و هر دو در SQLite و Log دیده شوند.

---

## فاز 6 — سرعت کار و کیبورد
- [x] 6.1 ترتیب صحیح Tab
- [x] 6.2 ثبت با Enter
- [x] 6.3 Popup سبز «ثبت شد»
- [x] 6.4 دکمه «تأیید» Popup
- [x] 6.5 بستن Popup با کیبورد
- [x] 6.6 برگشت Focus به شرح
- [ ] 6.7 تست چند ثبت پشت‌سرهم
- [x] 6.8 Commit

**Done when:** چند ثبت متوالی تقریباً بدون استفاده از ماوس انجام شود.

---

## فاز 7 — ثبت‌های اخیر
- [x] 7.1 نمایش آخرین 5 یا 10 ثبت همان User
- [x] 7.2 نمایش تاریخ
- [x] 7.3 نمایش نوع
- [x] 7.4 نمایش شرح
- [x] 7.5 نمایش مبلغ
- [x] 7.6 نمایش ساعت
- [x] 7.7 دکمه ویرایش
- [x] 7.8 جلوگیری از ویرایش ثبت دیگران
- [x] 7.9 ذخیره Before/After در Log
- [x] 7.10 دکمه حذف
- [x] 7.11 Confirm حذف
- [x] 7.12 جلوگیری از حذف ثبت دیگران
- [x] 7.13 Log کامل حذف
- [x] 7.14 Commit

**Done when:** User فقط رکوردهای خودش را ویرایش/حذف کند و همه تغییرات Audit شوند.

---

## فاز 8 — دفتر روزانه Admin
- [x] 8.1 صفحه گزارش فقط برای Admin
- [x] 8.2 جدول تاریخ
- [x] 8.3 جدول شرح
- [x] 8.4 ستون بدهکار
- [x] 8.5 ستون بستانکار
- [x] 8.6 Expense → Debit
- [x] 8.7 Income → Credit
- [x] 8.8 جمع بدهکار
- [x] 8.9 جمع بستانکار
- [x] 8.10 فیلتر بازه تاریخ
- [x] 8.11 اعداد فارسی
- [x] 8.12 تاریخ شمسی
- [x] 8.13 Commit

**Done when:** Admin دفتر روزانه صحیح را برای یک بازه تاریخ مشاهده کند.

---

## فاز 9 — چاپ A4/A5
- [x] 9.1 قالب چاپ A4
- [ ] 9.2 تست حاشیه‌ها
- [ ] 9.3 تست صفحه‌بندی
- [x] 9.4 قالب چاپ A5
- [ ] 9.5 تست صفحه‌بندی A5
- [x] 9.6 RTL چاپ
- [x] 9.7 فارسی بودن تاریخ و اعداد
- [ ] 9.8 تست Print Preview
- [x] 9.9 Commit

**Done when:** یک گزارش نمونه روی A4 و A5 خوانا و صحیح باشد.

---

## فاز 10 — صفحه Audit Log
- [x] 10.1 صفحه Log فقط Admin
- [x] 10.2 نمایش تاریخ/ساعت
- [x] 10.3 نمایش Actor
- [x] 10.4 نمایش Event
- [x] 10.5 نمایش رکورد مرتبط
- [x] 10.6 نمایش نتیجه
- [x] 10.7 نمایش Before/After برای تغییرات
- [x] 10.8 فیلتر بر اساس User
- [x] 10.9 فیلتر بر اساس Event
- [x] 10.10 فیلتر بازه زمانی
- [x] 10.11 قابلیت پاک‌سازی فقط Admin
- [x] 10.12 Log کردن خود عملیات پاک‌سازی
- [x] 10.13 Commit

**Done when:** مسیر کامل یک ثبت از ایجاد تا ویرایش/حذف قابل ردیابی باشد.

---

## فاز 11 — Backup
- [x] 11.1 تعریف مسیر پیش‌فرض Backup
- [x] 11.2 Backup دستی
- [x] 11.3 Log موفقیت Backup دستی
- [x] 11.4 Log خطای Backup دستی
- [x] 11.5 Backup خودکار
- [x] 11.6 تنظیم مسیر سفارشی
- [x] 11.7 تست مسیر سفارشی سالم
- [x] 11.8 شبیه‌سازی unavailable بودن مسیر
- [x] 11.9 Fallback به مسیر پیش‌فرض
- [x] 11.10 Log کردن Fallback
- [x] 11.11 هشدار به Admin
- [x] 11.12 تست Restore از یک Backup
- [x] 11.13 Commit

**Done when:** حتی با خراب بودن مسیر سفارشی، Backup معتبر در مسیر پیش‌فرض تولید شود.

---

## فاز 12 — تست شبکه و چندکاربره
- [ ] 12.1 تست 2 کاربر همزمان
- [ ] 12.2 تست 3 کاربر همزمان
- [ ] 12.3 تست 5 کاربر همزمان
- [x] 12.4 ثبت همزمان چند رکورد
- [x] 12.5 بررسی عدم تداخل داده
- [ ] 12.6 تست Login/Logout همزمان
- [x] 12.7 تست قفل اکانت
- [ ] 12.8 تست قطع موقت شبکه
- [ ] 12.9 تست اتصال مجدد
- [x] 12.10 Commit

**Done when:** 5 کاربر داخل LAN بتوانند بدون خرابی اطلاعات کار کنند.

---

## فاز 13 — نهایی‌سازی رابط
- [x] 13.1 بررسی همه صفحات RTL
- [x] 13.2 بررسی همه اعداد فارسی
- [x] 13.3 بررسی همه تاریخ‌ها شمسی
- [x] 13.4 بررسی وسط‌چین بودن Labelها
- [x] 13.5 بررسی وسط‌چین بودن Inputها
- [x] 13.6 بررسی Headerها
- [ ] 13.7 تست موبایل
- [ ] 13.8 تست Desktop
- [x] 13.9 حذف عناصر اضافی
- [x] 13.10 Commit

**Done when:** UI ساده، سریع، فارسی و یکدست باشد.

---

## فاز 14 — بسته نصب و تحویل
- [x] 14.1 تعیین ساختار نهایی پوشه‌ها
- [x] 14.2 تعیین محل فایل DB
- [x] 14.3 تعیین محل Backup پیش‌فرض
- [x] 14.4 تنظیم راه‌اندازی سرور
- [ ] 14.5 تست اجرای برنامه پس از Restart
- [ ] 14.6 تست از کلاینت شبکه
- [x] 14.7 نوشتن راهنمای نصب
- [x] 14.8 نوشتن راهنمای Admin
- [ ] 14.9 گرفتن Backup اولیه
- [ ] 14.10 Tag/Version نهایی
- [ ] 14.11 Commit نهایی

**Done when:** برنامه روی سیستم مقصد نصب و از چند دستگاه شبکه قابل استفاده باشد.

---

## فاز 15 — تغییر پورت و تنظیمات سرور
- [x] 15.1 تغییر پورت پیش‌فرض از ۵۰۰۰ به ۴۰۰۰
- [x] 15.2 ذخیره پورت در جدول Settings
- [x] 15.3 افزودن صفحه تنظیمات فقط برای Admin
- [x] 15.4 اعتبارسنجی بازه پورت ۱۰۲۴ تا ۶۵۵۳۵
- [x] 15.5 اعمال پورت ذخیره‌شده پس از Restart
- [x] 15.6 به‌روزرسانی فایروال و راهنماها
- [x] 15.7 اجرای تست‌ها و Commit تغییرات

**Done when:** پورت ۴۰۰۰ پیش‌فرض باشد و مدیر بتواند پورت معتبر دیگری را برای اجرای بعدی ذخیره کند.

---

## فاز 16 — نصب استاندارد Windows
- [x] 16.1 جداسازی فایل‌های برنامه در Program Files از داده‌های قابل‌نوشتن در ProgramData
- [x] 16.2 بسته‌بندی Python و وابستگی‌ها داخل خروجی PyInstaller
- [x] 16.3 تعریف Windows Service با اجرای خودکار و بازیابی پس از خطا
- [x] 16.4 افزودن میانبر Start Menu/Desktop و تنظیم خودکار Firewall
- [x] 16.5 پیاده‌سازی Upgrade و Uninstall با حفظ پیش‌فرض داده‌ها
- [x] 16.6 افزودن ساخت یک‌کلیکی Setup روی Windows x64
- [ ] 16.7 ساخت فایل نهایی `DailyBook-Setup-x64.exe` روی Windows
- [ ] 16.8 تست Clean Install، Upgrade و Uninstall روی Windows 10/11

**Done when:** یک فایل Setup مستقل، بدون نیاز کاربر نهایی به Python یا تنظیم دستی، روی Windows نصب و حذف شود.

---

## فاز 17 — اصلاحات رابط نسخه rc4
- [x] 17.1 انتقال تاریخ به سمت چپ و نوع هزینه/درآمد به سمت راست فرم ثبت
- [x] 17.2 وسط‌چین کردن عنوان‌ها، دکمه ثبت و عنوان ۱۰ ثبت اخیر
- [x] 17.3 تیره‌تر کردن پس‌زمینه باکس‌ها و افزایش کادرها به ۲ پیکسل در کل برنامه
- [x] 17.4 افزودن ساعت و نام کامل کاربر و خطوط کامل سلول‌ها به گزارش دفتر
- [x] 17.5 بازطراحی هدر و افزودن منوی همبرگری هفت‌گزینه‌ای مدیر
- [x] 17.6 اجرای تست‌های خودکار و افزایش نتیجه موفق به ۳۱ تست
- [ ] 17.7 تأیید بصری نهایی تغییرات در مرورگر Desktop و موبایل

**Done when:** کاربر چیدمان جدید فرم، هدر، منو و گزارش را در مرورگر واقعی تأیید کند.

---

# 13) قواعد ادامه پروژه با ChatGPT

برای اینکه محدودیت زمانی، قطع اینترنت یا تغییر اکانت باعث از دست رفتن کار نشود:

1. **هیچ‌وقت Chat History را منبع اصلی پروژه در نظر نگیر.**
2. این فایل `PROJECT_MASTER.md` همیشه کنار سورس پروژه نگهداری شود.
3. فایل `STATUS.md` بعد از هر مرحله به‌روزرسانی شود.
4. بعد از هر Micro Task موفق، یک Commit محلی ایجاد شود.
5. تا وقتی کاربر نتیجه یک Task را تأیید نکرده، Task بعدی شروع نشود.
6. اگر تغییری در تصمیم‌های پروژه داده شد، اول این فایل اصلاح شود.
7. در هر جلسه جدید، ChatGPT باید اول:
   - PROJECT_MASTER.md
   - STATUS.md
   - و در صورت نیاز سورس/ZIP پروژه
   را بخواند.
8. هر جلسه باید با یک Checkpoint تمام شود:
   - چه چیزی انجام شد؟
   - چه فایل‌هایی تغییر کردند؟
   - چه چیزی تست شد؟
   - Task بعدی چیست؟
   - آیا Blocker وجود دارد؟
9. کدهایی که تست نشده‌اند به‌عنوان «انجام‌شده» تیک نخورند.
10. پس از هر فاز، یک ZIP Snapshot نیز می‌تواند ذخیره شود.

---

# 14) قالب STATUS.md

```md
# وضعیت فعلی پروژه

آخرین بروزرسانی:
نسخه:

## آخرین Task تأییدشده
- [x] ...

## Task فعلی
- [ ] ...

## سه Task بعدی
- [ ] ...
- [ ] ...
- [ ] ...

## فایل‌های تغییرکرده در آخرین مرحله
- ...

## تست‌های انجام‌شده
- ...

## خطاها / Blockerها
- ...

## تصمیم جدید
- ...
```

---

# 15) Decision Log

هر تصمیم جدیدی که در طول پروژه قطعی می‌شود، اینجا اضافه شود.

| تاریخ | تصمیم | وضعیت |
|---|---|---|
| فعلی | پروژه فقط LAN باشد | قطعی |
| فعلی | دیتابیس SQLite | قطعی |
| فعلی | اولین User = Admin اصلی | قطعی |
| فعلی | Admin تنها دارنده گزارش و Log کامل | قطعی |
| فعلی | User نمی‌تواند Password را تغییر دهد | قطعی |
| فعلی | Lock بعد از 5 Password اشتباه | قطعی |
| فعلی | Username باید Unique باشد | قطعی |
| فعلی | User می‌تواند Active/Inactive/Delete شود | قطعی |
| فعلی | حذف User نباید سابقه قبلی را حذف کند | قطعی |
| فعلی | فرم ثبت فقط تاریخ/نوع/شرح/مبلغ | قطعی |
| فعلی | تاریخ شمسی و اعداد فارسی | قطعی |
| فعلی | کل UI RTL و Center aligned | قطعی |
| فعلی | Popup سبز «ثبت شد» | قطعی |
| فعلی | پس از Popup، Focus روی شرح | قطعی |
| فعلی | 5 یا 10 ثبت اخیر همان User | قطعی |
| فعلی | گزارش کلاسیک بدهکار/بستانکار | قطعی |
| فعلی | چاپ A4 و A5 | قطعی |
| فعلی | Backup دستی + Auto Backup | قطعی |
| فعلی | Fallback Backup به مسیر پیش‌فرض | قطعی |
| فعلی | Audit Log جامع | قطعی |
| ۱۴۰۵/۰۶/۲۸ | Backend با Python و Flask اجرا شود | قطعی |
| ۱۴۰۵/۰۶/۲۸ | پورت پیش‌فرض شبکه ۵۰۰۰ باشد | لغوشده با تصمیم جدید کاربر |
| ۱۴۰۵/۰۶/۲۸ | پورت پیش‌فرض شبکه ۴۰۰۰ باشد و مدیر بتواند آن را از تنظیمات تغییر دهد | قطعی |
| ۱۴۰۵/۰۶/۲۸ | ۱۰ ثبت اخیر کاربر نمایش داده شود | قطعی |
| ۱۴۰۵/۰۶/۲۸ | حذف ثبت و کاربر به‌صورت Soft Delete انجام شود | قطعی |
| ۱۴۰۵/۰۶/۲۸ | بکاپ خودکار روزانه اجرا شود | قطعی |
| ۱۴۰۵/۰۶/۲۸ | فایل دیتابیس در `instance/dailybook.db` و بکاپ پیش‌فرض در `backups/` باشد | لغوشده برای نسخه نصب‌شده |
| ۱۴۰۵/۰۶/۲۸ | نصب ویندوز با BAT و اجرای LAN با Waitress انجام شود | لغوشده با تصمیم جدید کاربر |
| ۱۴۰۵/۰۶/۲۸ | نسخه فعلی 1.0.0-rc2 و آماده تست میدانی Windows/LAN باشد | جایگزین‌شده با rc3 |
| ۱۴۰۵/۰۶/۲۸ | نصب با یک Setup استاندارد و بدون پیش‌نیاز برای کاربر نهایی انجام شود | قطعی |
| ۱۴۰۵/۰۶/۲۸ | برنامه در Program Files و داده‌ها در ProgramData نگهداری شوند | قطعی |
| ۱۴۰۵/۰۶/۲۸ | اجرای سرور به‌صورت Windows Service خودکار باشد | قطعی |
| ۱۴۰۵/۰۶/۲۸ | نسخه 1.0.0-rc3 شامل منبع بسته‌بندی استاندارد Windows باشد | قطعی |
| ۱۴۰۵/۰۶/۲۸ | اصلاحات چیدمان فرم، کادرها، گزارش و منوی همبرگری در نسخه rc4 اعمال شوند | قطعی |

---

# 16) مواردی که هنوز باید تصمیم‌گیری شوند

این موارد عمداً باز گذاشته شده‌اند و نباید بدون تأیید کاربر قطعی شوند:

- Backend/Framework نهایی — تعیین شد: Python/Flask
- Port نهایی برنامه — تعیین شد: ۴۰۰۰
- نمایش دقیق 5 یا 10 رکورد اخیر — تعیین شد: ۱۰
- زمان‌بندی دقیق Auto Backup — تعیین شد: روزانه
- مسیر پیش‌فرض دقیق DB و Backup — نسخه نصب‌شده: `C:\ProgramData\DailyBook` و زیرپوشه `backups`
- ظاهر بصری نهایی — نسخه RC با فونت سیستمی و تم سبز/سفید پیاده‌سازی شد؛ تأیید میدانی باقی است
- رفتار دقیق حذف DailyEntry در دیتابیس — تعیین شد: Soft Delete + Log
- نحوه نصب و Auto Start روی Windows — Setup استاندارد و Windows Service تعیین شد

---

# 17) تعریف موفقیت نسخه 1.0

نسخه 1.0 زمانی آماده است که:

- Admin اولیه ساخته شود.
- Admin بتواند User مدیریت کند.
- User بتواند وارد شود.
- Lock پس از 5 خطای Login درست کار کند.
- User بتواند هزینه/درآمد ثبت کند.
- فرم کاملاً فارسی، RTL، شمسی و با اعداد فارسی باشد.
- ثبت با کیبورد سریع باشد.
- آخرین ثبت‌های همان User قابل مشاهده/ویرایش/حذف باشند.
- Admin دفتر روزانه بدهکار/بستانکار را ببیند.
- A4/A5 چاپ شوند.
- Log جامع کار کند.
- Backup دستی و خودکار کار کنند.
- Fallback Backup کار کند.
- برنامه با 3 تا 5 User همزمان در LAN پایدار باشد.

---

# 18) گزارش تحویل به اکانت/عامل بعدی

## وضعیت توقف

- پروژه در نسخه `1.0.0-rc4` و پس از اعمال اصلاحات رابط، برای انتقال و Build نهایی متوقف می‌شود.
- اصلاحات اعلام‌شده کاربر در فرم ثبت، کادرهای عمومی، گزارش و منوی مدیر اعمال شده‌اند.
- مرجع شروع، فایل `DailyBook-v1.0.0-rc4-source.zip` به‌همراه همین سند و `STATUS.md` است.

## کارهای انجام‌شده در این مرحله

- برنامه Flask/SQLite با رابط فارسی RTL، احراز هویت، مدیریت کاربران، دفتر روزانه، Audit Log و Backup پیاده‌سازی شد.
- پورت پیش‌فرض روی ۴۰۰۰ قرار گرفت و تغییر آن از پنل مدیر ممکن شد.
- مسیر نصب استاندارد به `C:\Program Files\DailyBook` و مسیر داده به `C:\ProgramData\DailyBook` منتقل شد.
- بسته‌بندی PyInstaller، Windows Service، Firewall، میانبرها، Upgrade و Uninstall در سورس نصب‌کننده آماده شد.
- فایل `BUILD_WINDOWS_SETUP.bat` برای Build یک‌کلیکی روی Windows x64 ساخته شد.
- فرم ثبت، کادرهای عمومی، گزارش دفتر و هدر/منوی مدیر طبق اصلاحات کاربر بازطراحی شد.
- ۳۱ تست خودکار موفق و compile کد بدون خطا ثبت شد.
- Commit اصلی اصلاحات رابط: `b4fea1a` با عنوان `refine daily entry and admin report UI`.

## وظایف عامل بعدی دارای دسترسی GitHub

1. ابتدا تأیید بصری اصلاحات rc4 را از کاربر بگیرد؛ بدون تأیید کاربر مستقیم سراغ Release نرود.
2. سورس نهایی را در یک مخزن خصوصی GitHub قرار دهد.
3. یک GitHub Actions Workflow با Windows x64 Runner بسازد.
4. در Workflow، تست‌ها و `installer/build_installer.ps1 -Clean` را اجرا کند.
5. فایل `installer/output/DailyBook-Setup-x64.exe` را به‌عنوان Artifact خروجی منتشر کند.
6. Setup را روی Windows 10/11 برای Clean Install، Upgrade، Restart، تغییر پورت و Uninstall آزمایش کند.
7. نتیجه تست‌های واقعی LAN و Windows را در `STATUS.md` ثبت کند.
8. فقط پس از موفقیت تست‌ها، نسخه نهایی را Tag/Release کند و Setup را به کاربر تحویل دهد.

## نکات مهم برای ادامه

- کاربر نهایی نباید Python، PowerShell یا فایل BAT اجرا کند؛ فقط Setup نهایی تحویل داده شود.
- GitHub فقط برای Build و Release است؛ برنامه در زمان استفاده فقط داخل LAN کار می‌کند.
- فایل دیتابیس هرگز داخل Program Files یا Share شبکه قرار نگیرد.
- پورت پیش‌فرض ۴۰۰۰ باقی بماند، مگر اینکه کاربر تصمیم دیگری بگیرد.
- هیچ‌کدام از Taskهای 16.7 و 16.8 تا زمان Build و تست واقعی Windows تیک نخورد.

---

**پایان سند مادر**


# 19) طراحی نهایی رابط — rc5

- طرح نهایی Dark/Light تأیید و روی سورس واقعی اعمال شد.
- Admin/Server: سایدبار ثابت سمت راست، بدون Hamburger، شامل خانه، ثبت روزانه، گزارش‌ها، کاربران، بکاپ و بازیابی، لاگ فعالیت‌ها، تنظیمات و خروج.
- User عادی: هیچ سایدباری ندارد؛ فقط فرم ثبت و ۱۰ ثبت اخیر متعلق به همان User نمایش داده می‌شود.
- مالکیت داده همچنان در Backend با `created_by_user_id == current_user.id` اعمال می‌شود و صفحات Admin برای User عادی مسدود هستند.
- Theme Toggle حالت Dark/Light را در `localStorage` نگه می‌دارد.
- فایل‌های اصلی تغییرکرده: `app/templates/base.html`، `app/templates/admin/_menu.html`، `app/templates/admin/base.html`، `app/templates/entries/dashboard.html`، `app/static/css/app.css`، `app/static/js/app.js` و `tests/test_admin.py`.
- نسخه Installer به `1.0.0-rc5` افزایش یافت.
- اجرای pytest در محیط فعلی به دلیل نبود Flask و نبود دسترسی شبکه برای نصب dependency ممکن نشد؛ تست مرورگر واقعی و اجرای suite کامل روی محیط دارای dependency باقی است.


## تصمیم رابط rc5 — ۱۴۰۵/۰۶/۲۹
- طرح دارای Sidebar ثابت فقط مخصوص حساب Admin روی کامپیوتر سرور است.
- تشخیص کنسول سرور در وب از loopback (`127.0.0.1` / `::1`) انجام می‌شود؛ میانبر سرور باید localhost را باز کند.
- کاربران LAN سایدبار ندارند. User عادی فقط ثبت روزانه و ۱۰ رکورد اخیر خودش را می‌بیند.
- تم Dark/Light و Design System میان Admin و User مشترک است.

- [x] نمایش عنوان رویدادهای Audit Log در رابط مدیر به فارسی انجام شد؛ مقدار استاندارد انگلیسی در دیتابیس بدون تغییر باقی می‌ماند.
